# Implementation Plan: R-23-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-23**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-23 in these files?

## Proposed Changes

### R-23 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/services/AppLogger.ts:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (33315 bytes) - flag for mandatory component extraction
- *Fix:* Extract PII scrubbing or stats calculation to a separate utility file to reduce file size.

**[HIGH] src/services/DeviceRepository.ts:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (39286 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[LOW] src/services/CrewService.ts:1** (R-23)
- *Confidence:* 🟡 PROBABLE
- *Desc:* File exceeds the 30KB limit (30,718 bytes), marking it as a monolith candidate for extraction.
- *Fix:* Extract Telemetry or Session State logic into separate service modules to reduce file size and complexity.

**[HIGH] src/protocols/SpatialEngine.ts:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (60544 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic and chunk/pattern builders into smaller sub-modules.

**[HIGH] src/protocols/ZenggeProtocol.ts:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (53397 bytes) - flag for mandatory component extraction
- *Fix:* Extract protocol response parsing, constants, or specific commands into separate helper files

**[HIGH] src/components/DockedController.tsx:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (67686 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to separate files to reduce size below 30KB

**[HIGH] src/components/docked/UniversalSlidersFooter.tsx:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (33695 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[MEDIUM] src/components/AccountModal.tsx:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (30866 bytes) - flag for mandatory component extraction
- *Fix:* Extract the inline AccountModalSkeleton and internal complex styling into separate files, or extract specific Tab renders.

**[HIGH] src/screens/DashboardScreen.tsx:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (51383 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[HIGH] src/screens/Onboarding/HardwareSetupWizardScreen.tsx:1** (R-23)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* File exceeds 30KB limit (41306 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[MEDIUM] src/components/VisualizerUnit.tsx:1** (R-23)
- *Confidence:* 🟡 PROBABLE
- *Desc:* File exceeds 30KB limit (30.5KB).
- *Fix:* Extract path geometry logic and complex LED rendering into separate pure functions or sub-components.

**[HIGH] src/components/crew/CrewDetailScreen.tsx:1** (R-23)
- *Confidence:* 🟡 PROBABLE
- *Desc:* File exceeds 30KB limit (32185 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[HIGH] src/components/crew/CrewLandingScreen.tsx:1** (R-23)
- *Confidence:* 🟡 PROBABLE
- *Desc:* File exceeds 30KB limit (36610 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

**[HIGH] src/types/supabase.ts:1** (R-23)
- *Confidence:* 🟡 PROBABLE
- *Desc:* File exceeds 30KB limit (150641 bytes) - flag for mandatory component extraction
- *Fix:* Extract logic/components to smaller files to bring size under 30KB

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
