# Implementation Plan: sweep-src-hooks

## Goal
Fix bugs and rule violations identified during the deep-dive code hunt for the `sweep-src-hooks` domain cluster.

## Batch & Wave
- **Wave:** 4
- **Prerequisite:** Wave 3 fully merged

## Proposed Changes
### [MODIFY] useAdminTelemetry.ts
- Line 126: Fix `R-03` violation. Calls setIsUploading(false) in finally block after an async network operation without checking if the component/hook remains mounted, leading to memory leaks and state warnings. (Suggested: Check if isMountedRef.current is true before calling setIsUploading(false).)
- Line 100: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 113: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useBLE.ts
- Line 327: Fix `R-09` violation. Logs raw device MAC address (deviceId) in AppLogger payloads for notification parse failures. (Suggested: Wrap deviceId in scrubPII(deviceId).)
- Line 224: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 308, 312: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 364: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 398: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 454: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 108: Fix `R-08` violation. Uses 'any' type for hwCache Record definition and setOnHardwareProbed callback definitions. (Suggested: Use Record<string, PingResult> or define a structured type.)
- Line 138: Fix `R-16` violation. Raw setTimeout is used to delay the connection request to restored BLE peripherals on boot to avoid immediate GATT 133 conflicts. (Suggested: Manage peripheral reconnection scheduling through the XState BleMachine or a centralized queue-managed sequence rather than an unmanaged setTimeout.)
### [MODIFY] useFavorites.ts
- Line 157: Fix `R-12` violation. saveFavorite callback references the user state variable but is missing user from its dependency array, leading to a stale closure. (Suggested: Add 'user' to the useCallback dependency array.)
- Line 207: Fix `R-12` violation. deleteFavorite callback references the user state variable but is missing user from its dependency array, leading to a stale closure. (Suggested: Add 'user' to the useCallback dependency array.)
- Line 182: Fix `R-21` violation. Split-brain architecture and lack of offline queue sync. Favorites cloud sync, save, and delete are handled directly inside the React hook instead of services, bypassing offline queue mechanisms completely. (Suggested: Move favorites sync logic into a centralized FavoritesService (matching ScenesService/GradientsService architecture) and integrate it with useOfflineSyncWorker's flushing mechanisms.)
- Line 50: Fix `R-11` violation. Empty catch block when parsing saved favorites from storage, swallowing parse exceptions. (Suggested: Ensure exceptions are handled by falling back to defaults or logging properly.)
- Line 67: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 97, 101: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 107: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 119: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 125: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 178: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 199: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 217: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 228: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 242: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useGradients.ts
- Line 52: Fix `R-12` violation. saveGradient function defined in functional hook without useCallback, causing reference changes on every render. (Suggested: Wrap saveGradient in a useCallback hook with dependency array [userId, loadGradients, telemetry].)
- Line 67: Fix `R-12` violation. deleteGradient function defined in functional hook without useCallback, causing reference changes on every render. (Suggested: Wrap deleteGradient in a useCallback hook with dependency array [userId, loadGradients].)
### [MODIFY] useDashboardCrew.ts
- Line 82: Fix `R-12` violation. A stable callback (onApplyScene) is captured in a persistent subscription closure inside useEffect but excluded from dependency array. Changes in callback reference cause stale state executions. (Suggested: Store the onApplyScene callback in a mutable React.useRef object and execute through it in the closure.)
### [MODIFY] useCrewManage.ts
- Line 65: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 76: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 92: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useDashboardProfile.ts
- Line 89, 94: Fix `R-11` violation. The Promise returned by AppSettingsService.fetchAllSettings() in the useEffect hook on mount is not handled with a .catch() block, which can cause unhandled promise rejections if the API or AsyncStorage fails. (Suggested: Add a .catch() handler to log the error and degrade gracefully, e.g., '.catch((e: unknown) => AppLogger.warn("useDashboardProfile", "Failed to fetch settings", e instanceof Error ? e.message : String(e)))'.)
- Line 116: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 126: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 141: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useMusicMode.ts
- Line 131: Fix `R-12` violation. The transition useEffect that sends the music exit packet captures stale state values (musicPrimaryColor, musicSecondaryColor, musicPatternId, etc.) from closure because they are omitted from the dependency array. (Suggested: Track the latest parameters in React refs on every render and read from those refs in the transition useEffect to avoid stale closures without triggering updates on param changes.)
- Line 84: Fix `R-09` violation. PII key 'patternId' passed with unscrubbed value 'safePatternId' in AppLogger call. (Suggested: Wrap value in scrubPII(safePatternId) if it contains sensitive user information.)
### [MODIFY] useAppMicrophone.ts
- Line 59: Fix `R-26` violation. An async re-entrancy race condition exists in startRecording / stopRecording. If activeMode transitions quickly, stopRecording may finish before startRecording resolves its async promises, leading startRecording to continue, start the microphone, and stream magnitudes in the background indefinitely. (Suggested: Introduce checks after each async await boundary in startRecording to verify if activeMode / micSource is still 'MUSIC' / 'APP' before continuing to record.)
- Line 91: Fix `R-22` violation. A leaked interval will be created if useAppMicrophone is unmounted while startRecording is in the middle of async permission checks. stopRecording runs synchronously during unmount, but startRecording will subsequently set the interval after the component is already unmounted. (Suggested: Verify that isMountedRef.current is true immediately before launching the recorder and setting magnitudeInterval.current.)
- Line 46: Fix `R-18` violation. Boolean Trap: isCapturingRef is a mutable boolean ref that tracks recording state in parallel with the recorder.isRecording or recorder.getStatus().isRecording state, creating duplicate sources of truth for the recording status. (Suggested: Eliminate the isCapturingRef flag and rely directly on the recorder's native recording state queries to guard start and stop routines.)
- Line 98: Fix `R-20` violation. Imports 'Audio' from 'expo-av' and creates a new 'Audio.Recording()' instance within startRecording() without checking if the platform is Web. On React Native Web, this constructor is unsupported and will crash the application during runtime. (Suggested: Wrap the instantiation in a Platform check: if (Platform.OS === 'web') return; or wrap the recording logic in a robust try/catch.)
### [MODIFY] useStreetMode.ts
- Line 216: Fix `R-09` violation. Spreading deviceContextRef.current directly into the AppLogger payload without filtering can leak hardware identifiers like the connected device MAC address. (Suggested: Scrub the hardware identification attributes (such as macAddress / deviceId) from the deviceContext object before spreading it into the AppLogger telemetry.)
- Line 193: Fix `R-17` violation. Non-surgical, unselective listener removal. Toggling STREET mode off calls Accelerometer.removeAllListeners() instead of removing only its own subscription via sub.remove(). This destroys all accelerometer listeners registered globally, including the listener in SensorService that tracks peak G-force for the active session, violating the surgical/ghost bounds (P4) and causing functional regression. (Suggested: Remove the Accelerometer.removeAllListeners() call and instead rely on the existing sub.remove() returned in the clean-up block to cleanly unregister only this hook's listener.)
- Line 81: Fix `R-18` violation. Boolean Trap: streetBrakingRef.current is a mutable boolean ref that tracks the hard braking state in parallel with the motionState/motionStateRef FSM, creating scattered states and synchronization/race condition risks. (Suggested: Refactor useStreetMode to rely entirely on motionState and motionStateRef for braking state, eliminating the duplicate streetBrakingRef boolean variable.)
### [MODIFY] useProductCatalog.ts
- Line 90: Fix `R-26` violation. Async functions loadCachedCatalog() and syncFromCloud() are called inside a useEffect hook without a re-entrancy guard. Rapid component mounting/unmounting can trigger concurrent executions of these async methods, causing duplicate state updates and redundant database query requests. (Suggested: Introduce a boolean re-entrancy ref (e.g., isSyncingRef) or cancel/ignore prior fetches if a new mount occurs before the previous async actions complete.)
- Line 113: Fix `R-07` violation. The syncFromCloud function is defined as a plain async function inside the useProductCatalog hook and returned directly without being wrapped in a useCallback. This causes a new function reference to be created on every render, which can lead to unnecessary re-renders in components or hooks that depend on it (e.g., useProductManager). (Suggested: Wrap syncFromCloud in a useCallback hook with appropriate dependencies.)
- Line 109: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 134: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 188: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useProductManager.ts
- Line 84, 86: Fix `R-06` violation. Catch block intercepts 'err' of type 'unknown' and passes it directly to AppLogger.error without standard unwrapping or verifying if it is an instance of Error. (Suggested: Unwrap 'err' before logging by checking if it is an instance of Error, e.g., 'err instanceof Error ? err : new Error(String(err))'.)
### [MODIFY] useProtocolDispatch.ts
- Line 115: Fix `R-11` violation. The executeRawPayload callback calls writeChunked() and returns the resulting promise directly without an internal catch block. If writeChunked fails and rejects the promise, this could cause an unhandled promise rejection if callers do not catch it. (Suggested: Add a catch block or wrap the call in a try/catch block to handle transmission failures gracefully, or document that the caller must handle promise rejections.)
### [MODIFY] useSessionTracking.ts
- Line 52: Fix `R-21` violation. Split-brain architecture and duplication of session recording lifecycle logic. Both useSessionTracking and useGlobalTelemetry independently manage session timer accumulation, speed sample rolling arrays, peak speed tracking, and distance accumulation. Having multiple hooks tracking the same session metrics introduces code drift, double counting, and synchronization inconsistencies. (Suggested: Consolidate session lifecycle tracking entirely into a single system (e.g., useGlobalTelemetry or the newer SessionMachine state manager) and remove useSessionTracking.ts from the active codebase.)
- Line 120: Fix `R-04` violation. Error logged via AppLogger.error without standard telemetry context properties (payload_size and ssi). (Suggested: Include payload_size: 0 and ssi: 0 in the metadata parameter of the AppLogger call.)
### [MODIFY] useGlobalTelemetry.ts
- Line 288: Fix `R-11` violation. Unawaited floating promise call on commitSession(). In the cleanup block of the main useEffect, the asynchronous commitSession() is invoked synchronously without an await statement or .catch() error boundary. If the asynchronous commit fails, it will trigger an unhandled promise rejection. (Suggested: Chain a .catch() block: commitSession().catch(err => AppLogger.error('[useGlobalTelemetry] Commit session failed', err));)
- Line 296: Fix `R-12` violation. Missing externalStartTimeMs in the useEffect dependency array. The effect initializes the session start time using externalStartTimeMs || Date.now(). Since externalStartTimeMs is not reactive within the effect dependencies, if it changes during setup, the effect closure will run with a stale value. (Suggested: Include externalStartTimeMs in the dependency array or map it to a mutable useRef that is updated on render.)
- Line 253: Fix `R-04` violation. Error logged via AppLogger.error without standard telemetry context parameters (payload_size and ssi). (Suggested: Pass payload_size: 0 and ssi: 0 inside the logging metadata.)
### [MODIFY] useHealthTelemetry.ts
- Line 72: Fix `R-26` violation. pollHealthData is a heavy asynchronous polling function that does not implement any concurrency lock. If a poll takes longer than the 15-second interval, multiple polling executions can overlap and concurrently update state variables, causing race conditions in the bpmSamplesRef.current array. (Suggested: Add an isPolling boolean flag or a ref-based lock to prevent a new poll from starting if a previous one is currently executing.)
- Line 112: Fix `R-04` violation. Warning logged via AppLogger.warn without standard telemetry context (payload_size and ssi). (Suggested: Append payload_size: 0, ssi: 0 to the logging context object.)
### [MODIFY] useTelemetryLedger.ts
- Line 94: Fix `R-26` violation. flushToDatabase is called on heartbeat intervals and in the AppState listener when backgrounding. If a heartbeat-triggered flush is currently in flight (network request), the AppState change listener will call flushToDatabase and exit immediately because _isFlushing is true, leading to data loss of the newly accumulated telemetry data. (Suggested: Chain new flush requests onto the active flush Promise, or introduce a dirty/pending flag that triggers a follow-up flush when the active one finishes, especially during AppState transitions to ensure all data is written before background suspension.)
- Line 106: Fix `R-26` violation. flushToDatabase has no concurrency lock. If multiple concurrent callers enter the flush flow (e.g. from backgrounding or timer triggers), both will read the same offline queue from AsyncStorage, send duplicate RPC calls to Supabase, and concurrently clear the storage buffer, causing duplicate database records. (Suggested: Implement an isFlushing boolean flag or ref-based lock that instantly blocks concurrent executions of the flush process.)
- Line 176: Fix `R-17` violation. Module-level global timer globalFlushTimer is cleared in the hook's cleanup function. Since this timer is shared across all instances of the hook, if any single component mounting the hook is unmounted, it will clear the global interval, stopping the background flush heartbeat for all other active components consuming the hook. (Suggested: Track active listener instances with a ref counter, or scope the interval lifecycle to a centralized context provider rather than inside a raw hook instance cleanup.)
- Line 191: Fix `R-17` violation. Event listener for AppState changes is registered globally inside a check block without returning a cleanup function in the useEffect hook, and the flush callback is registered to a shared global Set without cleanup, leading to memory/closure leaks on unmount. (Suggested: Store the event listener subscription returned by AppState.addEventListener, register the flush callback inside useEffect, and return a cleanup function to unsubscribe the listener, remove the callback from _registeredFlushCallbacks, and stop the shared timer on unmount.)
- Line 148: Fix `R-04` violation. Warning logged via AppLogger.warn without standard telemetry context (payload_size and ssi). (Suggested: Add payload_size: 0 and ssi: 0 to the logged context.)
- Line 188: Fix `R-22` violation. AppState listener and global 15-minute heartbeat timer are registered on first hook mount without unmount cleanups. Although they use module-level flags to ensure they are only registered once (singleton pattern), hook hygiene suggests returning cleanup functions to prevent test leakage and stale references. (Suggested: Store the AppState subscription returned by AppState.addEventListener and clean it up inside a useEffect return. Also remove the callback from _registeredFlushCallbacks and call stopSharedTimer on unmount, or refactor useTelemetryLedger to be powered by a dedicated context provider with a proper lifecycle.)
### [MODIFY] useDeviceStateLedger.ts
- Line 83: Fix `R-26` violation. warmLedgerCache() and load() perform asynchronous operations on AsyncStorage without re-entrancy locks, causing redundant I/O reads if invoked concurrently on app startup or rapid loading. (Suggested: Add a promise-level in-flight cache check or a boolean lock to prevent concurrent AsyncStorage reads.)
- Line 46: Fix `R-08` violation. Explicit use of the any type on the global Node/Browser object (const globalAny: any = global;) to store Dev-mode Fast Refresh cache indicators, bypassing strict type safety. (Suggested: Extend the Global interface declaration or use type-safe accessors.)
- Line 53: Fix `R-08` violation. Global scope bypass to attach ledger cache and timers. Bypasses the type checker to set custom map properties directly on 'global'. (Suggested: Declare global namespace augmentation in a declaration file (e.g. '.d.ts') or use 'declare global { var __sk8lytz_ledger_cache: Map<string, DevicePatternState> | undefined; }' to register the properties with TypeScript.)
- Line 162: Fix `R-09` violation. Device MAC address (classified as hardware/physical PII) is logged unredacted inside AppLogger.error context object under the key mac: key. (Suggested: Redact the MAC address using a utility function like scrubPII(key) or only log the last 4 characters.)
### [MODIFY] useControllerAnalytics.ts
- Line 57: Fix `R-09` violation. Device MAC addresses (via deviceId and deviceIds fields inside deviceContext) are logged directly to the AppLogger telemetry service during state changes without normalization or hashing, exposing PII. (Suggested: Strip or hash the deviceId/deviceIds fields from the deviceContext object before passing it to AppLogger, or pass only truncated or sanitized device identifiers.)
### [MODIFY] useControllerDispatch.ts
- Line 71: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 90: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 123: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 194: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 249: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 305: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 363: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 385: Fix `R-04` violation. AppLogger.error call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.error(message, errorObj, { ...context, payload_size: 0, ssi: 0 });)
- Line 323, 325: Fix `R-10` violation. In handleMusicChange, writes are sent sequentially in a for...of loop with manual setTimeout delays instead of using concurrent Promise.all mapped writes for multi-device groups. (Suggested: Replace the serial loop with Promise.all-mapped concurrent writes, or delegate timing management to the lower-level BleWriteQueue.)
- Line 94: Fix `R-13` violation. UI dispatch hook useControllerDispatch.ts triggers multiple concurrent safeWrite operations in parallel using Promise.all when writing to group devices. While this resembles parallel GATT setups, the calls are safely serialized at the transport/queue level (BleWriteQueue & BleWriteDispatcher) using a FIFO queue and a 50ms inter-device write delay. (Suggested: No code action required. The system design relies on the transport layer (BleWriteQueue) singleton to serialize all dispatches sequentially. Documented to prevent automated linters or future refactors from flagging parallel patterns as violations.)
- Line 340: Fix `R-16` violation. Raw setTimeout wrapped in a Promise is used inside dispatchMusicConfig to introduce an inter-device write gap when sending music configuration packets, bypassing the BleWriteQueue. (Suggested: Replace the raw setTimeout with await enqueueDelay('normal', BLE_TIMING.INTER_DEVICE_WRITE_GAP_MS) to let the centralized BleWriteQueue serialize the gap.)
- Line 352: Fix `R-16` violation. Raw setTimeout wrapped in a Promise is used inside dispatchMusicConfig to stagger write gaps for music configuration commands across target devices. (Suggested: Replace the raw setTimeout with await enqueueDelay('normal', BLE_TIMING.INTER_DEVICE_WRITE_GAP_MS) to ensure the gap is queue-managed.)
- Line 10: Fix `R-21` violation. Overlapping command building responsibilities: Both 'useControllerDispatch.ts' and 'useProtocolDispatch.ts' contain translation logic mapping UI actions to BLE controller packets (e.g., solid colors, brightness). While useControllerDispatch wraps writes with optimistic UI state for the DockedController component, it duplicates some build/dispatch logic that exists globally in useProtocolDispatch. (Suggested: Refactor useControllerDispatch to delegate command array construction to useProtocolDispatch by resolving the BLE context correctly, or accept pre-built packet arrays from the UI caller directly to preserve the optimistic wrapper separation.)
### [MODIFY] useOfflineSyncWorker.ts
- Line 41: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useAccountOverview.ts
- Line 88, 93: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 117, 121: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 125: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 151: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 228, 233: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 277: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 294: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 178: Fix `R-06` violation. Passed raw exception variable "e" directly to logging call "AppLogger.error". (Suggested: Wrap the exception object or convert using a ternary: "e instanceof Error ? e.message : String(e)")
- Line 221: Fix `R-06` violation. Passed raw exception variable "e" directly to logging call "AppLogger.error". (Suggested: Wrap the exception object or convert using a ternary: "e instanceof Error ? e.message : String(e)")
- Line 307: Fix `R-06` violation. Passed raw exception variable "e" directly to logging call "AppLogger.error". (Suggested: Wrap the exception object or convert using a ternary: "e instanceof Error ? e.message : String(e)")
- Line 274: Fix `R-09` violation. PII key 'crewName' passed with unscrubbed value 'newCrewName.trim()' in AppLogger call. (Suggested: Wrap resource name in scrubPII(newCrewName.trim()) or log the unique ID instead of name.)
- Line 291: Fix `R-09` violation. PII key 'crewId' passed with unscrubbed value 'crew.id' in AppLogger call. (Suggested: Wrap value in scrubPII(crew.id) if it contains sensitive user information.)
- Line 78: Fix `R-26` violation. loadData fetches user profile details, crew list, and session history in parallel. It is triggered in a useEffect whenever the visible prop becomes true. Rapidly toggling visible will invoke multiple concurrent executions of loadData without cancelling in-flight queries, risking state updates out-of-order. (Suggested: Add an AbortController to loadData and abort previous pending requests on unmount/re-run, or use an active flag pattern to ignore previous query results if a new fetch is initiated.)
### [MODIFY] useAdminSettings.ts
- Line 31: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 54: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useCuratedPicks.ts
- Line 105: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useDashboardDeviceConfig.ts
- Line 82: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 121: Fix `R-09` violation. PII key 'deviceId' passed with unscrubbed value 'targetMac' in AppLogger call. (Suggested: Wrap value in scrubPII(targetMac) if it contains sensitive user information.)
- Line 134: Fix `R-09` violation. PII key 'oldName' passed with unscrubbed value 'previousName || 'Unknown'' in AppLogger call. (Suggested: Wrap resource name in scrubPII(previousName || 'Unknown') or log the unique ID instead of name.)
### [MODIFY] useDashboardGroups.ts
- Line 186: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 218: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 262, 264: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 270: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 361: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 379: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 409: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 478: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useDiagnosticLog.ts
- Line 91, 96: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 174: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 193: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 146: Fix `R-09` violation. PII key 'deviceId' passed with unscrubbed value 'targetDeviceId ?? undefined' in AppLogger call. (Suggested: Wrap value in scrubPII(targetDeviceId ?? undefined) if it contains sensitive user information.)
### [MODIFY] useMapFilters.ts
- Line 41: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 55: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useOptimisticBLE.ts
- Line 117: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useRecentSpots.ts
- Line 43: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 74: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 54: Fix `R-26` violation. addRecentSpot reads the recent spots list from AsyncStorage, appends the new spot, and writes it back. It is asynchronous but has no re-entrancy locks or queuing. If a user triggers addRecentSpot multiple times rapidly, it creates a read-modify-write race where some spots fail to persist. (Suggested: Implement a simple queue mechanism or sequential promise chain for AsyncStorage operations to ensure read-modify-write processes do not run in parallel.)
### [MODIFY] useRegistration.ts
- Line 109: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 144: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 160: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 174: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 187: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 214: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 223: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 237: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
### [MODIFY] useCrewSession.ts
- Line 66: Fix `R-09` violation. PII key 'crewName' passed with unscrubbed value 'currentSession?.name' in AppLogger call. (Suggested: Wrap resource name in scrubPII(currentSession?.name) or log the unique ID instead of name.)
- Line 107: Fix `R-09` violation. PII key 'sessionId' passed with unscrubbed value 'currentSession?.id' in AppLogger call. (Suggested: Wrap value in scrubPII(currentSession?.id) if it contains sensitive user information.)
### [MODIFY] useDashboardAutoConnect.ts
- Line 157: Fix `R-12` violation. The useEffect hook monitoring device scanning updates checks isWizardActive but does not include it in its dependency array. This can lock the connection process or cause it to ignore state transitions. (Suggested: Add isWizardActive to the useEffect dependency array.)
### [MODIFY] useProtocolBuilder.ts
- Line 80: Fix `R-18` violation. Boolean Trap: bldMic and setBldMic expose a boolean toggle API that wraps and maps the descriptive bldMicSource string union ('APP' | 'DEVICE'), introducing a boolean trap wrapper for state representation. (Suggested: Expose the descriptive string union bldMicSource directly to consumers and update UI control state to bind directly to the enum values.)
### [MODIFY] useCrewHub.ts
- Line 66: Fix `R-26` violation. useEffect triggers profileService.getCrewMembersForDisplay for any crew in myCrews that does not yet have a member count. Because crewMemberCounts is state that resolves asynchronously, rapid re-renders or changes to visible/myCrews in the meantime will re-trigger the effect and execute duplicate parallel requests for the same crew IDs. (Suggested: Maintain a Ref of currently in-flight crew fetches (e.g. fetchingCrewIdsRef.current = new Set()) and skip fetching if the crew ID is already in the Ref, removing it when the promise completes.)

## Verification Plan
- Run `npm run verify` to ensure type safety and tests pass.
- Run AST parser to ensure no regressions.
