# Implementation Plan: R-21-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-21**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-21 in these files?

## Proposed Changes

### R-21 Violations

The following issues must be resolved surgically:

**[LOW] package.json:78** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* @types/jest (^30.0.0) is out of sync with jest core (^29.7.0). This version overhang causes duplicate and conflicting type definitions.
- *Fix:* Downgrade @types/jest to ^29.0.0 to align with jest core, or upgrade jest to ^30.0.0.

**[MEDIUM] package.json:92** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* jest-expo version (~51.0.0) is out of sync with expo version (~55.0.8). This causes split-brain test environments where test mocks do not match runtime implementation.
- *Fix:* Update jest-expo to ~55.0.0 to match the expo version.

**[HIGH] src/services/NotificationService.ts:84** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Calling legacy profileService.registerPushToken instead of newly extracted pushTokenService. Missed during God Object Decomposition.
- *Fix:* Import and use pushTokenService.registerPushToken and unregisterPushToken.

**[MEDIUM] src/components/CustomSlider.tsx:46** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Split-Brain duplication of PanResponder drag calculation logic between CustomSlider and TacticalSlider.
- *Fix:* Extract the slider PanResponder logic into a shared hook (e.g., `useSliderPanResponder`).

**[HIGH] src/hooks/useControllerDispatch.ts:45** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Split-Brain Protocol Dispatch: useControllerDispatch duplicates BLE dispatch functionality from useProtocolDispatch. It hardcodes Zengge protocol bypasses (e.g., 0x59 FREEZE) and manages its own LRU pattern payload cache, circumventing the IControllerProtocol adapter layer used across the rest of the application.
- *Fix:* Refactor DockedController.tsx to consume useProtocolDispatch. Extract the 0x59 Temporal array generation into ZenggeAdapter and remove useControllerDispatch entirely to ensure a single source of truth for hardware translation.

**[MEDIUM] src/hooks/useBLE.ts:269** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Split-Brain Connected Devices: useBLE maintains a connectedDevicesRef and an updateConnectedDevices write-through setter to circumvent XState's 1-frame propagation delay from BleMachine. While documented, this creates two parallel state arrays tracking active BLE connections.
- *Fix:* Rely fully on the XState context for source-of-truth logic and use useSelector for React rendering, avoiding a dual-write sync architecture.

**[MEDIUM] src/hooks/useCrewSession.ts:17** (R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Split-Brain Crew Session State: useCrewSession duplicates currentSession and currentRole via useState from the CrewService class properties. It manually syncs React state with the singleton class property mutations.
- *Fix:* Convert CrewService to an EventEmitter or Zustand store so React components can subscribe to the singleton state directly without duplicating it in local useState hooks.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
