# Implementation Plan: R-03-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-03**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-03 in these files?

## Proposed Changes

### R-03 Violations

The following issues must be resolved surgically:

**[HIGH] src/services/BleSessionFactory.ts:127** (R-03)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing jitter in GATT 133 connection retry loop. Uses static [500, 1500] backoff without randomization, risking reconnect storms.
- *Fix:* Use jitteredDelay(delay, 500) from src/utils/backoff.ts

**[MEDIUM] src/services/BleWriteQueue.ts:205** (R-03)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Transient GATT error write retry loop lacks backoff and jitter. Uses hardcoded 100ms delay.
- *Fix:* Add randomized jitter to the 100ms delay to prevent collision storms on MTU congestion.

**[MEDIUM] src/services/ble/ConnectService.ts:208** (R-03)
- *Confidence:* 🟡 PROBABLE
- *Desc:* MTU negotiation retry loop lacks jitter and exponential backoff, using static MTU_RETRY_SETTLE_MS.
- *Fix:* Apply jitteredDelay(BLE_TIMING.MTU_RETRY_SETTLE_MS, 50) to the MTU retry settle time.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
