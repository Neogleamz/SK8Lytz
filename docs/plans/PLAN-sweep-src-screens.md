# Implementation Plan: sweep-src-screens

## Goal
Fix bugs and rule violations identified during the deep-dive code hunt for the `sweep-src-screens` domain cluster.

## Batch & Wave
- **Wave:** 6
- **Prerequisite:** Wave 5 fully merged

## Proposed Changes
### [MODIFY] HardwareSetupWizardScreen.test.tsx
- Line 39: Fix `R-08` violation. Casting registeredDevice object to 'Record<string, unknown>' in test assertion. (Suggested: Define temporary type or use bracket notation to access optional properties.)
### [MODIFY] DashboardScreen.tsx
- Line 1: Fix `R-23` violation. DashboardScreen.tsx is a monolithic file of 62KB (1292 lines), which exceeds the 30KB monolith detection threshold. While co-locating BLE state prevents async race conditions in this complex view, component extraction is recommended for non-stateful UI components. (Suggested: Extract smaller stateless sub-components or slabs (like MySkatesSlab, CrewHubSlab) and helper hooks to separate files outside the screen monolith.)
- Line 140, 145: Fix `R-08` violation. Usage of 'Record<string, any>' as the type for 'lastGroupPatterns' state hook. This disables TypeScript's compiler safety checks for group pattern data structures. (Suggested: Define a concrete interface representing the pattern snapshot state (e.g., GroupPatternSnapshot) and use it in the record definition.)
- Line 244, 245: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 251: Fix `R-08` violation. In the signature of 'onApplyCloudScene' callback hook, the 'scene' parameter type is annotated with 'any'. (Suggested: Replace 'any' with a strict Type definition mapping to the cloud scene attributes.)
- Line 520: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 600: Fix `R-08` violation. In the 'onSessionReady' callback hook mapping to the DashboardCrewPanel wrapper, the parameter 'lastScene' is typed as 'any'. (Suggested: Replace 'any' with the structured scene interface definition.)
- Line 814, 815: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 816: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 953: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 1083: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 1129: Fix `R-08` violation. Type safety bypass in main screen. Device arrays or dispatch functions are cast to bridge differences between 'Device' and 'DisplayDevice' types. (Suggested: Ensure the custom 'DisplayDevice' interface correctly extends 'Device' from react-native-ble-plx. Align the argument and state signatures in context providers and hooks so they do not require 'as unknown as' casts.)
- Line 282: Fix `R-21` violation. Comment outlines a known logic discrepancy where 'd.groupId' is not populated on DisplayDevice, but header layout checks (e.g. in DashboardHeader.tsx) still reference 'firstDevice?.groupId' to resolve groups. This indicates split-brain logic patterns. (Suggested: Standardize group ownership metadata retrieval to use a single source of truth (e.g., deviceConfigs array or ledger state mapping).)
- Line 344: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 350: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 601: Fix `R-09` violation. PII key 'expectedMacs' passed with unscrubbed value 'group.deviceIds' in AppLogger call. (Suggested: Wrap value in scrubPII(group.deviceIds) to secure raw MAC addresses.)
- Line 943: Fix `R-14` violation. The main DashboardScreen manages device registration loading and redirects to SETUP_WIZARD on empty, but does not provide any top-level error state handling in the viewState switch if the registration or profile load fails. (Suggested: Add an 'ERROR' case to the viewState switch statement, rendering a full-screen ErrorCard that allows re-triggering registration and profile queries.)
- Line 342: Fix `R-24` violation. Hardcoded AsyncStorage key bypassing storageKeys.ts registry. The key '@Sk8lytz_crewHubCollapsed' is hardcoded directly inside DashboardScreen.tsx for reading and writing crew hub UI collapse state, bypassing the centralized storageKeys.ts registry and documentation. (Suggested: Define export const STORAGE_CREWHUB_COLLAPSED = '@Sk8lytz_crewHubCollapsed'; in storageKeys.ts, document it in the Master Reference, and import/use it in DashboardScreen.tsx.)
- Line 84: Fix `R-27` violation. DashboardScreen consumes 4 React Contexts directly: AppConfigContext (via useAppConfig), ThemeContext (via useTheme), BLEContext (via React.useContext), and SessionContext (via useSession). This exceeds the recommended maximum context consumer depth of 3. (Suggested: Extract sub-components or delegate context consumption to hooks/sub-components to keep context consumer depth <= 3 for the main screen component.)
### [MODIFY] AuthScreen.tsx
- Line 72: Fix `R-08` violation. Explicitly casting caught error as 'any' type in catch block within handleLogin handler. (Suggested: Use type-safe catch bindings: change signature to 'catch (err: unknown)' or 'catch (err)' and perform type narrowing using 'err instanceof Error'.)
- Line 92: Fix `R-08` violation. Explicitly casting caught error as 'any' type in catch block within handleSignup handler. (Suggested: Utilize catch (err: unknown) instead of casting directly to 'any'.)
- Line 38: Fix `R-10` violation. AuthScreen lacks screen rendering telemetry on mount. It logs basic opening events but does not call markFullyDrawn() or register loading durations using the useScreenPerformance hook. (Suggested: Introduce useScreenPerformance('AuthScreen') hook and trigger markFullyDrawn() once initial form assets are fully rendered.)
- Line 81, 86: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 74: Fix `R-24` violation. Functional gap and dead code. The key STORAGE_LAST_EMAIL (@Sk8lytz_auth_last_email) is read in AuthScreen and AuthContext to pre-populate the login screen, but there is no call to AsyncStorage.setItem for this key anywhere in the codebase. The user's last email is never actually saved, rendering the pre-population logic dead/broken. (Suggested: Add an AsyncStorage.setItem call in the successful sign-in handler (e.g., in AuthFormSignIn.tsx) to persist the last successfully logged-in email to STORAGE_LAST_EMAIL.)
### [MODIFY] HardwareSetupWizardScreen.tsx
- Line 1: Fix `R-23` violation. File size is 42681 bytes, exceeding the 30KB monolith limit. It implements the entire first-time user experience (FTUE) setup wizard, which has 3 distinct steps. (Suggested: Break down each step (renderStep1, renderStep2, renderStep3) into independent sub-components placed under src/screens/Onboarding/components/.)
- Line 607: Fix `R-08` violation. The local variable 'configs' is explicitly typed with 'Record<string, any>', which suppresses strict type check verification. (Suggested: Change the type annotation from 'any' to a clean, typed model like 'Record<string, WizardDeviceConfig>'.)
- Line 664: Fix `R-08` violation. Explicit 'any' cast on caught errors in the save configs transaction catch block. (Suggested: Employ standard type narrowing on catch (err) to retrieve error details safely.)
- Line 717: Fix `R-08, R-06` violation. Explicit 'any' cast on caught error within secondary ping execution transaction catch block. (Suggested: Avoid 'any' and use standard check: err instanceof Error.)
- Line 240: Fix `R-04` violation. AppLogger.warn call is missing telemetry context parameters: payload_size, ssi. Rule R-04 mandate that payload_size and ssi must be provided for all telemetry entries. (Suggested: AppLogger.warn(message, { ...context, payload_size: 0, ssi: 0 });)
- Line 120: Fix `R-06` violation. Passed raw exception variable "e" directly to logging call "AppLogger.error". (Suggested: Wrap the exception object or convert using a ternary: "e instanceof Error ? e.message : String(e)")
- Line 241: Fix `R-09` violation. PII key 'deviceId' passed with unscrubbed value 'deviceMac' in AppLogger call. (Suggested: Wrap value in scrubPII(deviceMac) if it contains sensitive user information.)
- Line 675: Fix `R-09` violation. PII key 'deviceId' passed with unscrubbed value 'device.device_mac' in AppLogger call. (Suggested: Wrap value in scrubPII(device.device_mac) if it contains sensitive user information.)
- Line 695: Fix `R-09` violation. PII key 'deviceId' passed with unscrubbed value 'device.device_mac' in AppLogger call. (Suggested: Wrap value in scrubPII(device.device_mac) if it contains sensitive user information.)
- Line 103: Fix `R-10` violation. The fireOrientationTest method writes to selected devices sequentially using a for...of loop with await pingDevice, resulting in sequential blocking during setup orientation checks. (Suggested: Refactor the loop using Promise.all to dispatch concurrent ping requests to all selected devices: await Promise.all(selected.map(async (device) => { ... })).)
- Line 669: Fix `R-10` violation. The wizard setup completion logic executes hardware config changes and plays signature animations sequentially on all selected devices via a for...of loop with await pingDevice, causing sequential delays during the claiming process. (Suggested: Refactor the setup verification loop to execute concurrently using Promise.all mapped connections.)

## Verification Plan
- Run `npm run verify` to ensure type safety and tests pass.
- Run AST parser to ensure no regressions.
