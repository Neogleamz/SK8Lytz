# Implementation Plan: R-26-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-26**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-26 in these files?

## Proposed Changes

### R-26 Violations

The following issues must be resolved surgically:

**[HIGH] src/services/AppLogger.ts:232** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Re-entrancy race condition in ensureLoaded. Multiple concurrent log calls will trigger simultaneous AsyncStorage reads, potentially wiping the buffer.
- *Fix:* Implement a Promise-based lock: if (this.loadPromise) return this.loadPromise;

**[HIGH] src/hooks/useFavorites.ts:17** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect without a boolean re-entrancy guard
- *Fix:* Implement an _isFlushing or isMounted re-entrancy guard

**[HIGH] src/hooks/useFavorites.ts:40** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] android/sk8lytzWear/src/main/kotlin/com/neogleamz/sk8lytzwear/services/HealthTracker.kt:47** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Re-entrancy Race on startTracking. isTracking is set asynchronously after awaiting startExerciseAsync, which allows multiple coroutines to launch if startTracking is called rapidly, causing Duplicate ExerciseClient requests or crashes.
- *Fix:* Set isTracking = true synchronously before launching the coroutine, and set it back to false in the onFailure block if startExerciseAsync fails.

**[HIGH] src/components/account/SkaterStatsPanel.tsx:26** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/components/admin/AdminToolsModal.tsx:105** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/components/permissions/GranularPermissionsList.tsx:91** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/hooks/useCuratedPicks.ts:26** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/hooks/useMapFilters.ts:25** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/hooks/useRegistration.ts:88** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/screens/DashboardScreen.tsx:426** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

**[HIGH] src/components/CommunityModal.tsx:89** (R-26)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async function called from useEffect/setInterval without a boolean re-entrancy guard.
- *Fix:* Add a useRef boolean guard (e.g. _isFlushingRef) to prevent re-entrancy races.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
