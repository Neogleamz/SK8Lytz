# Implementation Plan: R-13-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-13**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-13 in these files?

## Proposed Changes

### R-13 Violations

The following issues must be resolved surgically:

**[HIGH] src/services/BleWriteDispatcher.ts:307** (R-13)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Promise.all used to parallelize per-device GATT writes. This causes GATT 133 collisions on Android when communicating with multiple devices.
- *Fix:* Replace Promise.all with a sequential for...of loop and add an inter-device delay between each device's writes to prevent buffer overflow.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
