# Implementation Plan: R-24-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-24**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-24 in these files?

## Proposed Changes

### R-24 Violations

The following issues must be resolved surgically:

**[LOW] src/services/BleCharacteristicCache.ts:12** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage Key used directly; potentially undocumented key collision
- *Fix:* Ensure '@sk8_gatt_' is documented in Master Reference §2 AsyncStorage Key Registry or move to constants

**[MEDIUM] src/hooks/useDashboardProfile.ts:100** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage Key Collision / Hardcoded key used outside registry.
- *Fix:* Extract key to src/constants/storageKeys.ts.

**[MEDIUM] src/providers/ComplianceGate.tsx:35** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Hardcoded AsyncStorage key bypassing src/constants/storageKeys.ts.
- *Fix:* Move key to src/constants/storageKeys.ts and import it.

**[HIGH] src/constants/storageKeys.ts:1** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Multiple undocumented AsyncStorage keys defined in storageKeys.ts that are missing from Master Reference §2 AsyncStorage Key Registry (e.g. @Sk8lytz_offline_skip, @Sk8lytz_remember_creds, @Sk8lytz_demo_halo, @Sk8lytz_last_group_patterns, etc.)
- *Fix:* Audit and document all keys in the Master Reference §2, or remove unused keys.

**[LOW] src/screens/DashboardScreen.tsx:632** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision risk for '@Sk8lytz_Favorites'
- *Fix:* Use a centralized constant for AsyncStorage keys

**[LOW] src/utils/migrateAuthTokens.ts:4-5** (R-24)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Hardcoded AsyncStorage key instead of centralized constants
- *Fix:* Move key string to src/constants/storageKeys.ts

**[MEDIUM] src/components/auth/AuthFooterActions.tsx:31** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for undocumented key '@Sk8lytz_offline_skip'. Key is duplicated across 3 files (AuthFooterActions.tsx, AuthFormSignIn.tsx, AuthContext.tsx).
- *Fix:* Centralize all access into a dedicated Service or Context and add the key to Master Reference.

**[MEDIUM] src/components/auth/AuthFormSignIn.tsx:87** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for undocumented key '@Sk8lytz_remember_creds'. Key is duplicated across 3 files (AuthFormSignIn.tsx, DevSandboxDrawer.tsx, AuthScreen.tsx).
- *Fix:* Centralize all access into a dedicated Service or Context and add the key to Master Reference.

**[MEDIUM] src/components/auth/DevSandboxDrawer.tsx:19** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for undocumented key '@Sk8lytz_demo_mode'. Key is duplicated across 3 files (DevSandboxDrawer.tsx, useBLE.ts, ConnectService.ts).
- *Fix:* Centralize all access into a dedicated Service or Context and add the key to Master Reference.

**[MEDIUM] src/components/auth/DevSandboxDrawer.tsx:100** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for undocumented key '@Sk8lytz_auth_last_email'. Key is duplicated across 3 files (DevSandboxDrawer.tsx, AuthContext.tsx, AuthScreen.tsx).
- *Fix:* Centralize all access into a dedicated Service or Context and add the key to Master Reference.

**[MEDIUM] src/context/SessionContext.tsx:192** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for undocumented key '@Sk8lytz_auto_pause_enabled'. Key is duplicated across 2 files (SessionContext.tsx, useAccountOverview.ts).
- *Fix:* Centralize all access into a dedicated Service or Context and add the key to Master Reference.

**[LOW] src/hooks/ble/useBLEScanner.ts:74** (R-24)
- *Confidence:* 🟡 PROBABLE
- *Desc:* AsyncStorage key collision for key '@Sk8lytz_app_settings'. Key is duplicated across 2 files (useBLEScanner.ts, AppLogger.ts).
- *Fix:* Refactor useBLEScanner and AppLogger to use AppSettingsService instead of calling AsyncStorage directly.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
