# Implementation Plan: R-10-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-10**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-10 in these files?

## Proposed Changes

### R-10 Violations

The following issues must be resolved surgically:

**[HIGH] src/services/BleWriteDispatcher.ts:147** (R-10)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Sequential group write detected in loop instead of concurrent Promise.all mapping
- *Fix:* Replace sequential for-loop with Promise.all(liveTargets.map(async device => ...)) to ensure concurrent writes. Note: The codebase has a comment explaining this prevents GATT 133, so it may need refactoring rather than a blind Promise.all swap.

**[HIGH] src/services/BleWriteDispatcher.ts:208** (R-10)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Sequential group write detected in executeWriteChunked instead of concurrent Promise.all mapping
- *Fix:* Replace sequential inner device loop with Promise.all mapped concurrent writes, keeping chunk sequencing outer loop if required.

**[MEDIUM] src/services/ble/HeartbeatService.ts:30** (R-10)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Sequential group writes during heartbeat instead of concurrent mapped writes
- *Fix:* Map connectedDevices to an array of async write closures and await Promise.all() to broadcast heartbeats concurrently to all connected devices.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
