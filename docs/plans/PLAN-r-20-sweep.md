# Implementation Plan: R-20-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-20**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-20 in these files?

## Proposed Changes

### R-20 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/theme/theme.ts:71-75** (R-20)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Platform.select for Shadows.soft is missing a default/web fallback and casted as ViewStyle. Will crash/undefined on unsupported platforms.
- *Fix:* Add a `default: {}` fallback to the Platform.select object to ensure it always returns a valid ViewStyle.

**[MEDIUM] src/theme/theme.ts:79** (R-20)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Platform.select for Shadows.glow is missing a default/web fallback.
- *Fix:* Add a `default: {}` fallback.

**[HIGH] src/screens/DashboardScreen.tsx:22** (R-20)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Blind cross-platform assumption: Importing `SafeAreaView` from `react-native` instead of `react-native-safe-area-context`. The core `SafeAreaView` only pads for iOS notches and ignores Android entirely, causing UI to render under the status bar on Android devices.
- *Fix:* Remove `SafeAreaView` from `react-native` import and instead `import { SafeAreaView } from 'react-native-safe-area-context';`

**[HIGH] src/components/CommunityModal.tsx:8** (R-20)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Blind cross-platform assumption: Importing `SafeAreaView` from `react-native` instead of `react-native-safe-area-context`. The core `SafeAreaView` only pads for iOS notches and ignores Android entirely.
- *Fix:* Remove `SafeAreaView` from `react-native` import and instead `import { SafeAreaView } from 'react-native-safe-area-context';`

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
