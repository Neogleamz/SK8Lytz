# Implementation Plan: R-15-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-15**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-15 in these files?

## Proposed Changes

### R-15 Violations

The following issues must be resolved surgically:

**[LOW] supabase/functions/notify-crew-session/index.ts:27** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.getUser() call instead of using AuthContext.
- *Fix:* None needed. This is a false positive because Edge Functions do not have React Context.

**[HIGH] src/services/AppLogger.ts:658** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.getUser() call bypasses AuthContext and executes an unnecessary network request. Services should accept user/userId as arguments from the caller or store it via a setter.
- *Fix:* Pass userId as a parameter to uploadLogsToSupabase or inject the session externally.

**[MEDIUM] src/components/AccountModal.tsx:331** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.signInWithPassword() bypasses AuthContext's signIn method.
- *Fix:* Use the signIn method exposed by useAuth().

**[MEDIUM] src/components/AccountModal.tsx:425** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.signOut() bypasses AuthContext's signOut method.
- *Fix:* Use the signOut method exposed by useAuth().

**[MEDIUM] src/components/auth/DevSandboxDrawer.tsx:97** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.signOut() bypasses AuthContext's signOut method.
- *Fix:* Use the signOut method exposed by useAuth().

**[MEDIUM] src/providers/ComplianceGate.tsx:114** (R-15)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Direct supabase.auth.signOut() bypasses AuthContext's signOut method.
- *Fix:* Use the signOut method exposed by useAuth().

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
