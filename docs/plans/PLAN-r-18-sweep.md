# Implementation Plan: R-18-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-18**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-18 in these files?

## Proposed Changes

### R-18 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/hooks/useScenes.ts:9** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useStreetMode.ts:77** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Boolean Trap: `isStreetBraking` is managed alongside the `motionState` FSM union, leading to scattered states.
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[LOW] src/hooks/useProtocolBuilder.ts:76** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Boolean trap used for mic source selection
- *Fix:* Replace boolean toggle with a descriptive string union like 'DEVICE' | 'APP'

**[MEDIUM] src/screens/DashboardScreen.tsx:178-180** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/screens/DashboardScreen.tsx:187** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/screens/DashboardScreen.tsx:304** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/screens/DashboardScreen.tsx:473** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[MEDIUM] src/components/ProductVisualizer.tsx:38** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Boolean trap: isStreetBraking boolean exists alongside motionState string union, leading to conflicting states.
- *Fix:* Remove isStreetBraking and rely entirely on the motionState FSM.

**[MEDIUM] src/components/auth/DevSandboxDrawer.tsx:16** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/components/CrewModal.tsx:92** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/components/docked/BuilderPanel.tsx:54** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/components/docked/CameraPanel.tsx:55** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[HIGH] src/components/DockedController.tsx:215** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/components/DockedController.tsx:506** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[MEDIUM] src/components/GroupSettingsModal.tsx:20** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/components/LocationPicker.tsx:39** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[HIGH] src/components/SkateSpotBottomSheet.tsx:21-24** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[MEDIUM] src/context/ThemeContext.tsx:26** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/ble/useBLEBatterySweep.ts:27** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useAdminSettings.ts:10** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useAdminTelemetry.ts:79** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useAppMicrophone.ts:44** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[HIGH] src/hooks/useBLE.ts:162-163** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/hooks/useBLE.ts:164** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/hooks/useCrewManage.ts:25-26** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[MEDIUM] src/hooks/useCrewSession.ts:19** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useDashboardController.tsx:99** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useDashboardCrew.ts:39** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[HIGH] src/hooks/useDashboardGroups.ts:278** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[HIGH] src/hooks/useDashboardGroups.ts:297-298** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

**[MEDIUM] src/hooks/useDockedControllerState.ts:102** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useFavorites.ts:19** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useProductManager.ts:40** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/hooks/useRecentSpots.ts:17** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/providers/BluetoothGuard.tsx:16** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[MEDIUM] src/screens/AuthScreen.tsx:48** (R-18)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Single boolean state which may need FSM refactoring if complexity grows
- *Fix:* Replace boolean with state enum/union if this represents mutually exclusive UI phases

**[HIGH] src/screens/Onboarding/HardwareSetupWizardScreen.tsx:53-55** (R-18)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Scattered boolean state (Boolean Trap) used instead of FSM state union
- *Fix:* Replace isolated booleans with a consolidated FSM state union (e.g., status: 'idle' | 'loading' | 'success' | 'error')

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
