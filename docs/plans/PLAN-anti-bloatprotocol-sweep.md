# Implementation Plan: Anti-Bloat Protocol-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **Anti-Bloat Protocol**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to Anti-Bloat Protocol in these files?

## Proposed Changes

### Anti-Bloat Protocol Violations

The following issues must be resolved surgically:

**[MEDIUM] package.json:30** (Anti-Bloat Protocol)
- *Confidence:* 🟡 PROBABLE
- *Desc:* dotenv is a Node.js specific package listed in client dependencies. Expo handles environment variables natively. This is unnecessary bundle bloat.
- *Fix:* Remove dotenv from dependencies, rely on Expo's built-in env handling.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
