# Implementation Plan: R-06-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-06**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-06 in these files?

## Proposed Changes

### R-06 Violations

The following issues must be resolved surgically:

**[MEDIUM] supabase/functions/notify-crew-session/index.ts:108** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch blocks.
- *Fix:* catch (e) { const error = e instanceof Error ? e : new Error(String(e)); console.error(`[notify-crew-session] Network fetch failed:`, error); }

**[HIGH] src/services/ScenesService.ts:66** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Empty catch block swallowing errors completely
- *Fix:* Add error logging with e instanceof Error unwrapping

**[MEDIUM] src/services/AuthProfileService.ts:107** (R-06)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Missing standard `e instanceof Error` unwrapping in catch block before throw.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  // ... properly check error ...
}

**[LOW] src/providers/BluetoothGuard.tsx:22** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Empty catch block missing standard e instanceof Error unwrapping/logging.
- *Fix:* Add catch (e: unknown) and log the error before setting state.

**[MEDIUM] src/hooks/useDashboardController.tsx:162** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Empty catch block ignoring network promise rejection
- *Fix:* Add proper error logging and 'e instanceof Error' unwrapping

**[HIGH] src/components/CommunityModal.tsx:109** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  setError('Failed to load. Tap to retry.');
}

**[MEDIUM] src/hooks/dev/useWebDemoConsoleBridge.ts:31** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  return String(arg);
}

**[LOW] src/hooks/dev/useWebDemoConsoleBridge.ts:43** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  // Silently fail
}

**[MEDIUM] src/services/AppLogger.ts:246** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  this.buffer = [];
}

**[LOW] src/services/LocationService.ts:91** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  return null;
}

**[LOW] src/protocols/ZenggeProtocol.ts:192** (R-06)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing standard e instanceof Error unwrapping in catch block.
- *Fix:* } catch (err: unknown) {
  const e = err instanceof Error ? err : new Error(String(err));
  // Not JSON format - fall through to binary parse
}

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
