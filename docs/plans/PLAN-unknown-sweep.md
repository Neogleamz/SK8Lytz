# Implementation Plan: Unknown-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **Unknown**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to Unknown in these files?

## Proposed Changes

### Unknown Violations

The following issues must be resolved surgically:

**[undefined] src/hooks/useDashboardProfile.ts:0** (Unknown)
- *Confidence:* 🟡 PROBABLE
- *Desc:* useDashboardProfile receives an onCrewJoinNotification prop function which is recreated every render in DashboardScreen (it captures setPendingJoinCrewId and setIsCrewModalVisible state setters, and can capture any dynamic state). However, it is captured directly in an empty-dependency useEffect that registers it to notificationService.setJoinHandler. Since it has no useRef forwarding, if the callback relied on updated state, it would execute with stale initial closure data. Standard React convention dictates forwarding dynamically-created callback props via a stable ref before passing to external event listeners.
- *Fix:* const onCrewJoinNotificationRef = useRef(onCrewJoinNotification);
useEffect(() => { onCrewJoinNotificationRef.current = onCrewJoinNotification; }, [onCrewJoinNotification]);

notificationService.setJoinHandler((crewId) => onCrewJoinNotificationRef.current(crewId));

**[undefined] unknown:0** (Unknown)
- *Confidence:* ⚪ UNCERTAIN
- *Desc:* undefined
- *Fix:* undefined

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
