# Implementation Plan: R-09-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-09**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-09 in these files?

## Proposed Changes

### R-09 Violations

The following issues must be resolved surgically:

**[HIGH] src/services/LocationService.ts:64** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Reverse-geocoded address label logged to AppLogger, potentially leaking residential PII.
- *Fix:* Omit the raw address label from telemetry or mask it.

**[HIGH] src/hooks/useHardwareNotifications.ts:93** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* deviceId logged directly to AppLogger, leaking the raw BLE MAC address on Android.
- *Fix:* Hash or truncate the deviceId before logging to telemetry.

**[HIGH] src/services/ble/InterrogatorService.ts:148** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* MAC address is interpolated directly into the AppLogger string message, bypassing the PII scrubber.
- *Fix:* Wrap the mac variable in scrubPII(): `${scrubPII(mac)}`

**[HIGH] src/components/DockedController.tsx:448** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Array of MAC addresses passed to AppLogger. The internal PII scrubber fails to redact strings within arrays, causing a MAC leak.
- *Fix:* Map the IDs using scrubPII: `macs: devicesToReplay.map(x => scrubPII(x.id))`

**[HIGH] src/hooks/useDashboardAutoConnect.ts:195** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Array of device names and MAC addresses passed to AppLogger under the key 'devices'. The key 'devices' bypasses the scrubber check, and the scrubber also fails to redact array elements.
- *Fix:* Map using scrubPII for IDs and a separate scrubber for names, or change the key to match PII_KEY_PATTERNS and fix the array scrubber in AppLogger.

**[HIGH] src/hooks/useDashboardAutoConnect.ts:207** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Array of MAC addresses passed to AppLogger under the key 'macs'. The internal PII scrubber fails to redact primitive strings within arrays, causing a MAC leak.
- *Fix:* Map the IDs using scrubPII: `macs: batch.map(d => scrubPII(d.id))`

**[MEDIUM] src/hooks/useDashboardAutoConnect.ts:372** (R-09)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Array of fleet names passed to AppLogger. The internal PII scrubber fails to redact arrays of strings and 'fleets' bypasses the key regex, leaking potentially identifying custom group names.
- *Fix:* Do not log group names, or run them through a name scrubber.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
