# Implementation Plan: R-05-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-05**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-05 in these files?

## Proposed Changes

### R-05 Violations

The following issues must be resolved surgically:

**[HIGH] src/hooks/useSkateStats.ts:15** (R-05)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Bypassing AsyncStorage caching for data that should work offline
- *Fix:* Check local cache first before fetching from cloud

**[HIGH] src/services/SpeedTrackingService.ts:193** (R-05)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Bypasses AsyncStorage offline queue (PENDING_SESSION_QUEUE_KEY) for AUTHENTICATED users. If offline but authenticated, the Supabase insert fails and the session is permanently lost.
- *Fix:* Queue the session in AsyncStorage (PENDING_SESSION_QUEUE_KEY) on Supabase insertion error for authenticated users, just like unauthenticated users, to ensure offline-first reliability.

**[HIGH] src/services/SpeedTrackingService.ts:392** (R-05)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Bypasses AsyncStorage caching for historical skate sessions. Fallback only reads pending queue, not previously synced sessions.
- *Fix:* Cache fetched sessions to AsyncStorage and read from it when offline or prior to Supabase response.

**[HIGH] src/services/SpeedTrackingService.ts:472** (R-05)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Bypasses AsyncStorage caching for lifetime stats, making it unavailable offline. Directly accesses Supabase without local cache fallback.
- *Fix:* Fetch from AsyncStorage first, perform background Supabase sync, then update cache and state. Fall back to local cache if offline.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
