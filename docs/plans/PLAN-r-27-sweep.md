# Implementation Plan: R-27-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-27**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-27 in these files?

## Proposed Changes

### R-27 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/components/DockedController.tsx:190** (R-27)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Component DockedController directly consumes 4 React Contexts (useAppConfig, useTheme, useSharedBLE, useSharedFavorites). Every context change triggers a re-render of this monolithic component. Flagged for useMemo wrapping or context consolidation.
- *Fix:* Extract context consumers into an intermediate container component and pass values as memoized props, or consolidate shared contexts.

**[MEDIUM] src/screens/DashboardScreen.tsx:77-78** (R-27)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Component DashboardScreen directly consumes 4 React Contexts (useAppConfig, useTheme, BLEContext, useSession). Every context change triggers a re-render of this monolithic screen. Flagged for useMemo wrapping or context consolidation.
- *Fix:* Extract context consumers into an intermediate container component and pass values as memoized props, or consolidate shared contexts.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
