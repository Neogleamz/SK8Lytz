# Implementation Plan: Anti-Bloat Protocol / R-21-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **Anti-Bloat Protocol / R-21**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to Anti-Bloat Protocol / R-21 in these files?

## Proposed Changes

### Anti-Bloat Protocol / R-21 Violations

The following issues must be resolved surgically:

**[HIGH] package.json:51** (Anti-Bloat Protocol / R-21)
- *Confidence:* 🟡 PROBABLE
- *Desc:* pg (PostgreSQL Node.js client) is included in React Native client dependencies. It relies on Node core modules and is redundant (Split-Brain) since @supabase/supabase-js is already installed for database access.
- *Fix:* Remove 'pg' from dependencies.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
