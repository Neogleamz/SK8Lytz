# Implementation Plan: R-07-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-07**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-07 in these files?

## Proposed Changes

### R-07 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/components/VerticalPatternDrum.tsx:73** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* renderItem useCallback dependencies (commitValue, scrollToValue) are recreated every render, breaking memoization. Also inline functions inside renderItem.
- *Fix:* Extract Item component to its own React.memo component or use refs to avoid binding volatile dependencies into renderItem.

**[HIGH] src/components/VerticalPatternDrum.tsx:80** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline arrow function in onPress inside FlatList renderItem.
- *Fix:* Extract item component and pass stable callback.

**[MEDIUM] src/components/patterns/GradientLibraryTab.tsx:45** (R-07)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Inline functions and styles in FlatList renderItem and configuration props.
- *Fix:* Extract the render item to a separate React.memo component and move FlatList inline styles to StyleSheet.create.

**[HIGH] src/screens/DashboardScreen.tsx:828** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline arrow function in onPress inside FlatList renderItem causes component re-creation on every render.
- *Fix:* Extract list item into a separate React.memo component and pass stable useCallback references.

**[HIGH] src/components/crew/CrewJoinScreen.tsx:70** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline arrow function in onPress inside FlatList renderItem breaks memoization.
- *Fix:* Extract to memoized component or use a stable callback passing the ID.

**[HIGH] src/components/CommunityModal.tsx:188** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline arrow function in onPress inside FlatList renderItem breaks memoization.
- *Fix:* Extract item to a memoized component with stable callbacks.

**[HIGH] src/components/docked/FavoritesPanel.tsx:55** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline arrow function in onPress inside FlatList renderItem.
- *Fix:* Extract to memoized item component.

**[MEDIUM] src/components/patterns/PatternPickerTab.tsx:115** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline style object in FlatList renderItem creates new object reference every render.
- *Fix:* Move style definition to StyleSheet.create.

**[MEDIUM] src/components/admin/AdminToolsModal.tsx:163** (R-07)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Inline style object in FlatList renderItem.
- *Fix:* Memoize style object or pass to an extracted pure component.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
