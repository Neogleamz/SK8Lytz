# Implementation Plan: R-22-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-22**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-22 in these files?

## Proposed Changes

### R-22 Violations

The following issues must be resolved surgically:

**[MEDIUM] src/services/CrewService.ts:730** (R-22)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Dangling setTimeout reference: this._lastScenePersistTimer is never cleared in the unsubscribe() cleanup method.
- *Fix:* Add `if (this._lastScenePersistTimer) clearTimeout(this._lastScenePersistTimer);` to the unsubscribe() method.

**[MEDIUM] src/context/SessionContext.tsx:204** (R-22)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async race condition in checkAutoPause. If the effect unmounts during await AsyncStorage.getItem, the cleanup function runs while timer is null. The setTimeout is then created and can fire 10s later, erroneously changing sessionPhase after the session is stopped.
- *Fix:* Add an `let isActive = true` flag to the effect. Check `if (!isActive) return;` before assigning the timer. Set `isActive = false` in the cleanup.

**[HIGH] src/context/SessionContext.tsx:332** (R-22)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async race condition causing setInterval leak. setupNotification is an async function. If the effect unmounts during an await (e.g., displayNotification), the cleanup runs while updateInterval is null. The 5-second interval is then created and runs forever.
- *Fix:* Add an `let isActive = true` flag to the effect. Check `if (!isActive) return;` before assigning the setInterval. Set `isActive = false` in the cleanup.

**[HIGH] src/components/VerticalPatternDrum.tsx:36** (R-22)
- *Confidence:* 🟡 PROBABLE
- *Desc:* commitTimeoutRef is set but never cleared on component unmount, causing a potential memory leak.
- *Fix:* Add a useEffect cleanup function that calls clearTimeout(commitTimeoutRef.current).

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
