# Implementation Plan: R-17-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-17**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-17 in these files?

## Proposed Changes

### R-17 Violations

The following issues must be resolved surgically:

**[HIGH] src/hooks/useDashboardCrew.ts:86** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Race condition in useEffect cleanup for async tryRejoin() can leak Realtime channel subscriptions if unmounted before promise resolves.
- *Fix:* Use a boolean flag (let isMounted = true) and check it before setting unsub, or await the promise in a wrapped async function that immediately calls the returned unsubscribe if !isMounted.

**[HIGH] src/hooks/useGlobalTelemetry.ts:222** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Async race condition causing Location subscription leak. If isSkateSessionActive flips to false during the await Location.watchPositionAsync() call, the cleanup function runs while locationSubRef.current is null. The subscription is then created and never removed.
- *Fix:* Add an `let isActive = true` flag to the effect. Check `if (!isActive) { locationSubRef.current.remove(); }` immediately after the await. Set `isActive = false` in the cleanup function.

**[HIGH] src/components/VerticalPatternDrum.tsx:98** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing cleanup for listener/subscription in useEffect
- *Fix:* Return a cleanup function from useEffect to clear the timeout.

**[HIGH] src/hooks/useControllerAnalytics.ts:89** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing cleanup for timeout in useEffect
- *Fix:* Return a cleanup function from useEffect to clear the timeout.

**[HIGH] src/hooks/useControllerAnalytics.ts:97** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing cleanup for timeout in useEffect
- *Fix:* Return a cleanup function from useEffect to clear the timeout.

**[HIGH] src/hooks/useControllerAnalytics.ts:106** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing cleanup for timeout in useEffect
- *Fix:* Return a cleanup function from useEffect to clear the timeout.

**[HIGH] src/hooks/useDashboardAutoConnect.ts:247** (R-17)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing cleanup for timeout in useEffect
- *Fix:* Return a cleanup function from useEffect to clear the debounceTimerRef timeout.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
