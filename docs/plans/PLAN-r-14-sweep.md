# Implementation Plan: R-14-sweep

Provide a brief description of the problem, any background context, and what the change accomplishes.
This is an automatically synthesized plan addressing violations of rule **R-14**.

## User Review Required

> [!IMPORTANT]
> The following findings were automatically generated by the QA Fleet. Review them to ensure no false positives were missed during the synthesis phase before approving implementation.

## Open Questions

- Should any of these be deferred to the icebox?
- Are there any domain-specific exceptions to R-14 in these files?

## Proposed Changes

### R-14 Violations

The following issues must be resolved surgically:

**[HIGH] src/components/DockedController.tsx:401-402** (R-14)
- *Confidence:* 🔴 CONFIRMED
- *Desc:* Missing Error state handling for useCuratedPicks hook.
- *Fix:* Destructure `error` from `useCuratedPicks()` and provide fallback UI when fetching fails.

**[MEDIUM] src/components/LocationPicker.tsx:34** (R-14)
- *Confidence:* 🟡 PROBABLE
- *Desc:* Missing Loading and Error UI state handling for useRecentSpots hook.
- *Fix:* Destructure `isLoading` and `error` from `useRecentSpots` and display a loading indicator or error state in the LocationPicker chip area.

## Verification Plan

### Automated Tests
- `npm run verify` to ensure TypeScript and Jest tests pass after the cleanup.

### Manual Verification
- Walk through the affected UI screens to ensure no regressions.
