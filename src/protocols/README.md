# 🚨 TRIPWIRE: The Protocol Sandbox

> **CRITICAL AI CONTEXT**: You have entered the BLE Hardware Domain. 
> You are strictly **FORBIDDEN** from guessing, hallucinating, or relying on your base training data for any byte-math, hex payloads, or opcodes in this directory.

## The "Cite Your Truth" Mandate
Before you write or modify ANY code in `src/protocols/`, you MUST:
1. Execute `grep_search` or `view_file` on `tools/ZENGGE_PROTOCOL_BIBLE.md`.
2. Locate the specific opcode (e.g., `0x59`, `0x51`, `0x71`) you are working on.
3. Read the exact byte-array formula into your active memory.
4. In your Implementation Plan, include a `# Cited Truth` section quoting the exact line number from the Protocol Bible that justifies your code.

If you cannot find the answer in the Protocol Bible, you must HALT and ask the user. **DO NOT GUESS.**
