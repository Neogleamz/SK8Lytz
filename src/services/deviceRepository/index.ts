import DeviceRepositoryService from './DeviceRepositoryService';

// Default export acts as the singleton provider
export default DeviceRepositoryService;

// Re-export types if external consumers need them
export * from './types';
